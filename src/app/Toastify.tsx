'use client';

import { ToastContainer, toast } from 'react-toastify';

const Toastify = () => {
  return <ToastContainer position={toast.POSITION.TOP_RIGHT} theme="dark" />;
};

export default Toastify;
